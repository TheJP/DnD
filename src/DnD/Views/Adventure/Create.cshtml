@model DnD.Models.Adventure

@{
    ViewData["Title"] = "Create Adventure";
}

@await Html.PartialAsync("_Form", Model, new ViewDataDictionary(ViewData) { ["Button"] = "Create", ["Icon"] = "add" })

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
